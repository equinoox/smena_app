// Route: /(tabs)/listings — thin wrapper, renders the role-aware listings screen.
import { ListingsScreen } from "@features/listings/screens/ListingsScreen";

export default function ListingsRoute() {
  return <ListingsScreen />;
}

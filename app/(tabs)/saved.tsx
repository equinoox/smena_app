// Route: /(tabs)/saved — thin wrapper, renders the saved listings screen.
import { SavedScreen } from "@features/saved/screens/SavedScreen";

export default function SavedRoute() {
  return <SavedScreen />;
}

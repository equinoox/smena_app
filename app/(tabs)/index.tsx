// Route: /(tabs) home — thin wrapper, renders the role-aware home screen.
import { HomeScreen } from "@features/home/screens/HomeScreen";

export default function HomeRoute() {
  return <HomeScreen />;
}

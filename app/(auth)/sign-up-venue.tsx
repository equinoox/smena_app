// Route: /(auth)/sign-up-venue — thin wrapper, renders the venue sign-up screen.
import { VenueSignUpScreen } from "@features/auth/screens/VenueSignUpScreen";

export default function VenueSignUpRoute() {
  return <VenueSignUpScreen />;
}

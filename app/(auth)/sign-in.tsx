// Route: /(auth)/sign-in — thin wrapper, renders the sign-in screen.
import { SignInScreen } from "@features/auth/screens/SignInScreen";

export default function SignInRoute() {
  return <SignInScreen />;
}

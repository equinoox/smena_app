// Route: /(auth)/sign-up-worker — thin wrapper, renders the worker sign-up screen.
import { WorkerSignUpScreen } from "@features/auth/screens/WorkerSignUpScreen";

export default function WorkerSignUpRoute() {
  return <WorkerSignUpScreen />;
}

// Route: /worker/[id] — thin wrapper, renders the venue-facing worker detail screen.
import { WorkerDetailScreen } from "@features/workers/screens/WorkerDetailScreen";

export default function WorkerDetailRoute() {
  return <WorkerDetailScreen />;
}

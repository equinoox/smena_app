// Route: /venue-create — thin wrapper, renders the add-venue wizard (modal).
import { AddVenueScreen } from "@features/profile/screens/AddVenueScreen";

export default function VenueCreateRoute() {
  return <AddVenueScreen />;
}

// Route: /listing-create — thin wrapper, renders the venue's create-listing form.
import { CreateListingScreen } from "@features/listings/screens/CreateListingScreen";

export default function ListingCreateRoute() {
  return <CreateListingScreen />;
}

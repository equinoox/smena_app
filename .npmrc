# Expo Router pulls in web-only peer deps (radix-ui, vaul) that require a
# newer react-dom than the pinned react version. Applies to npm install and npm ci
# (including on EAS build servers) so builds don't fail on ERESOLVE conflicts.
legacy-peer-deps=true
